const path = require('path')

const BOT_EMOJI = '🤖'
const PREFIX = '//'
const TEMP_FOLDER = path.resolve(__dirname, '..', 'assets', 'temp')

module.exports = {
    BOT_EMOJI,
    PREFIX,
    TEMP_FOLDER
} 